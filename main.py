#Program Execution
import math_functions #we import the math function module using import statement

result = math_functions.add(5, 3)# we use add function from the math_functions to add two numbers (5, 3) and store the result in the variable result
print(result) # should be the sum of 5 and 3, resulting in the Output: 8
