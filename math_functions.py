#Module creation
#math_functions.py (module)
def add(x, y):# takes two params and returns their sum using + operator
    """Returns the sum of two numbers."""
    return x + y #returns sum

def subtract(x, y):#Takes two params x and y an returns their difference using the - operator
    """Returns the difference of two numbers."""
    return x - y